import AdminLayout from "../../components/AdminLayout";
import AdminPlaceholder from "../../components/AdminPlaceholder";
import { Plus } from "lucide-react";
const Page = () => (<AdminLayout title="Add Faculty" description="Create a new faculty"><AdminPlaceholder title="Add Faculty" icon={Plus} /></AdminLayout>);
export default Page;
