import AdminLayout from "../../components/AdminLayout";
import AdminPlaceholder from "../../components/AdminPlaceholder";
import { PlayCircle } from "lucide-react";
const Page = () => (<AdminLayout title="Start Session" description="Initialize a new academic session"><AdminPlaceholder title="Start Session" icon={PlayCircle} /></AdminLayout>);
export default Page;
